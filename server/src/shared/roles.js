/**
 * Shared role constants used by both server logic and emitted to clients.
 */

const ROLES = Object.freeze({
  DEVELOPER: 'Developer',
  HACKER: 'Hacker',
  SECURITY_LEAD: 'Security Lead',
  ADMIN: 'Admin',
});

module.exports = ROLES;
